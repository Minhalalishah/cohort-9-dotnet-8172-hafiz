Migrations are intentionally not committed in this starter scaffold. Run:

dotnet ef migrations add InitialCreate --project TaskManagement.Api
dotnet ef database update --project TaskManagement.Api

For a real submission, commit the generated migration files in this directory.
